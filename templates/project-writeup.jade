extends project-page

block prepend title
	| #{ page.title + " - "}

block append content-subhead
	| &raquo; 
	= page.parent.index.metadata.name

block append content
	!= typogr(page.html).typogrify()