
extends layout

include article-common

block append vars
	- bodyclass = 'article-detail'

block prepend title
	| #{ page.title + " - "}

block header
	h1= page.title

block content
	article.article
		+article_header(page)
		section.content!= typogr(page.html).typogrify()

block prepend footer
	div.nav
		a(href=contents.index.url) « Full blog


